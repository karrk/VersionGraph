<UserControl x:Class="VersionGraph.Views.GraphView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:graph="clr-namespace:VersionGraph.Graph"
             mc:Ignorable="d" d:DesignWidth="900" d:DesignHeight="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="12,10,12,6">
            <TextBlock Text="{Binding StatusText}" Opacity="0.7" DockPanel.Dock="Right"/>
            <TextBlock Text="{Binding RepoLabel}" FontSize="16" FontWeight="Bold"/>
        </DockPanel>

        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <graph:GraphCanvasControl Graph="{Binding Graph}"/>
        </ScrollViewer>
    </Grid>
</UserControl>
