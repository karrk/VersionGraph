<UserControl x:Class="VersionGraph.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="500" d:DesignHeight="300">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="360">
            <TextBlock Text="GitHub 로그인" FontSize="20" FontWeight="Bold" Margin="0,0,0,16"/>
            <TextBlock Text="버튼을 누르면 브라우저에서 GitHub 인증을 진행합니다." Margin="0,0,0,16"
                       Opacity="0.7" TextWrapping="Wrap"/>

            <Button Content="GitHub로 로그인" Command="{Binding LoginWithDeviceFlowCommand}" Height="32"
                    IsEnabled="{Binding IsAuthorizing, Converter={StaticResource InverseBoolConverter}}"/>

            <StackPanel Margin="0,16,0,0" Visibility="{Binding IsAuthorizing, Converter={StaticResource BoolToVisibleConverter}}">
                <TextBlock Text="브라우저에 표시된 페이지에 아래 코드를 입력하세요." HorizontalAlignment="Center" TextWrapping="Wrap"/>
                <TextBlock Text="{Binding UserCode}" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                <TextBlock Text="(코드는 클립보드에 복사되었습니다)" Opacity="0.6" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                <Button Content="취소" Command="{Binding CancelDeviceFlowCommand}" Margin="0,12,0,0" Height="28" Width="80"/>
            </StackPanel>

            <TextBlock Text="{Binding ErrorMessage}" Foreground="IndianRed" Margin="0,12,0,0"
                       TextWrapping="Wrap" Visibility="{Binding ErrorMessage, Converter={StaticResource NullToCollapsedConverter}}"/>
        </StackPanel>
    </Grid>
</UserControl>
