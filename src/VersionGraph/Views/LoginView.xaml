<UserControl x:Class="VersionGraph.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="500" d:DesignHeight="300">
    <Grid Background="{StaticResource BgBrush}">
        <Border Width="380" HorizontalAlignment="Center" VerticalAlignment="Center"
                Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource BorderBrush2}" BorderThickness="1"
                Padding="28,24">
            <StackPanel>
                <TextBlock Text="&gt; GITHUB AUTH" Style="{StaticResource HeadingText}"/>
                <Rectangle Style="{StaticResource SectionRule}" Margin="0,10,0,16"/>

                <TextBlock Text="버튼을 누르면 브라우저에서 GitHub 인증을 진행합니다." Style="{StaticResource DimText}"
                           Margin="0,0,0,18" TextWrapping="Wrap"/>

                <Button Content="LOGIN WITH GITHUB" Command="{Binding LoginWithDeviceFlowCommand}" Height="34"
                        IsEnabled="{Binding IsAuthorizing, Converter={StaticResource InverseBoolConverter}}"/>

                <StackPanel Margin="0,20,0,0" Visibility="{Binding IsAuthorizing, Converter={StaticResource BoolToVisibleConverter}}">
                    <TextBlock Text="브라우저에 표시된 페이지에 아래 코드를 입력하세요." Style="{StaticResource DimText}"
                               HorizontalAlignment="Center" TextWrapping="Wrap"/>
                    <Border BorderBrush="{StaticResource AccentBrush}" BorderThickness="1" Margin="0,10,0,0"
                            Padding="14,8" HorizontalAlignment="Center">
                        <TextBlock Text="{Binding UserCode}" FontSize="24" FontWeight="Bold"
                                   Foreground="{StaticResource AccentBrush}"/>
                    </Border>
                    <TextBlock Text="(코드는 클립보드에 복사되었습니다)" Style="{StaticResource DimText}" FontSize="11"
                               HorizontalAlignment="Center" Margin="0,8,0,0"/>
                    <Button Content="CANCEL" Command="{Binding CancelDeviceFlowCommand}" Margin="0,14,0,0"
                            Height="28" Width="100" HorizontalAlignment="Center"/>
                </StackPanel>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource ErrorBrush}" Margin="0,14,0,0"
                           TextWrapping="Wrap" Visibility="{Binding ErrorMessage, Converter={StaticResource NullToCollapsedConverter}}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
